# Tome Site Overhaul Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement `specs/2026-06-10-site-overhaul-design.md` — accurate Phase 8/9 docs in a four-chapter IA, the grimoire visual identity (parchment + midnight-library), a rebuilt six-section landing page with an auto-typing terminal demo, blog removal, and doc-drift prevention — as one branch / one PR.

**Architecture:** Docusaurus 3 classic preset stays. Content lands first (drift tooling → captured real CLI output → rewritten/new pages), then the IA flip (sidebars + deletions + blog removal) makes the build green, then the visual layer (custom.css tokens, Newsreader, landing components), then tests + CI.

**Tech Stack:** Docusaurus 3.10, React 19, TypeScript, CSS modules + Infima variables, `@fontsource-variable/newsreader`, Playwright, plain-Node scripts (`.mjs`) for drift tooling. No new runtime deps beyond the font package.

**Conventions for every task:**
- Repo root: `/Users/aaronbassett/Projects/devrel-ai/tome-site`. The CLI repo is the sibling `/Users/aaronbassett/Projects/devrel-ai/tome`.
- Installs MUST use Socket Firewall: `sfw pnpm add …` / `sfw pnpm remove …` (bare `pnpm add` is hard-blocked).
- Commits: Conventional Commits, `type(site): subject`.
- **Voice (spec §5):** task pages = motivation lede → worked example with real captured output → variations → pitfalls with exit codes → next steps. Concept pages = problem first, then model, one diagram. Reference = terse, tabular, complete; personality only in ledes/asides. Wry, concrete, bookish flourishes in headings/ledes only. **Never the words lend/lent/loan** — the metaphor is *bookshelf/read*: "add it to your bookshelf once, read it everywhere."
- **Honesty:** every terminal block in docs/landing is pasted from the captures in `specs/reference/captures/` (Task 3), never invented. Every command/flag/exit-code claim must exist in `specs/reference/cli-surface.json`.

---

### Task 1: Branch

**Files:** none (git only)

- [ ] **Step 1: Create the working branch**

```bash
cd /Users/aaronbassett/Projects/devrel-ai/tome-site
git checkout -b site-overhaul
```

Expected: `Switched to a new branch 'site-overhaul'`.

---

### Task 2: Drift tooling (failing first)

The contract + checker land before any content so the checker can prove the gap, then prove the fix. The command half of the contract is seeded here and regenerated from the real binary in Task 3; exit codes are hand-curated (they live in `error.rs`, not `--help`).

**Files:**
- Create: `specs/reference/cli-surface.json`
- Create: `scripts/generate-cli-surface.mjs`
- Create: `scripts/check-doc-drift.mjs`
- Modify: `package.json` (two script entries)

- [ ] **Step 1: Write the contract seed**

Create `specs/reference/cli-surface.json`. The `commands` half is a seed (Task 3 regenerates it from the binary — any drift between this seed and reality is overwritten there). The `exitCodes` half is authoritative and hand-curated; it is the current `docs/reference/exit-codes.md` table (codes 0–75, categories verbatim) **plus** the Phase 8/9 rows:

```json
{
  "$comment": "Command half generated by scripts/generate-cli-surface.mjs from a locally built tome binary. Exit codes hand-curated from tome's src/error.rs.",
  "commands": {
    "catalog": {"subcommands": {"add": [], "remove": ["--force"], "list": [], "update": [], "show": [], "create": ["--template", "--output", "--force"], "convert": ["--name", "--from", "--output", "--into", "--force", "--dry-run", "--strict", "--no-fetch"], "lint": ["--autofix", "--dry-run", "--strict"]}, "flags": []},
    "plugin": {"subcommands": {"enable": [], "disable": [], "list": [], "show": [], "create": ["--template", "--output", "--into", "--force"], "convert": ["--name", "--from", "--output", "--into", "--force", "--dry-run", "--strict"], "lint": ["--autofix", "--dry-run", "--strict"]}, "flags": []},
    "skill": {"subcommands": {"create": ["--template", "--bare", "--plugin-name", "--output", "--into", "--force"], "convert": ["--name", "--from", "--output", "--into", "--force", "--dry-run", "--strict"], "lint": ["--autofix", "--dry-run", "--strict"]}, "flags": []},
    "meta": {"subcommands": {"list": [], "add": ["--harness", "--global", "--force"], "remove": ["--harness", "--global"]}, "flags": []},
    "models": {"subcommands": {"download": [], "list": [], "remove": []}, "flags": []},
    "workspace": {"subcommands": {"use": [], "init": [], "list": [], "info": [], "rename": [], "regen-summary": [], "remove": [], "sync": []}, "flags": []},
    "harness": {"subcommands": {"use": [], "list": [], "info": [], "sync": [], "remove": []}, "flags": []},
    "query": {"subcommands": {}, "flags": ["--top-k", "--catalog", "--plugin", "--no-rerank", "--strict", "--min-score"]},
    "reindex": {"subcommands": {}, "flags": ["--force"]},
    "status": {"subcommands": {}, "flags": ["--verify", "--json"]},
    "doctor": {"subcommands": {}, "flags": ["--fix", "--force", "--verify", "--json"]},
    "mcp": {"subcommands": {}, "flags": ["--harness"]}
  },
  "globalFlags": ["--json", "--workspace"],
  "exitCodes": [
    {"code": 0, "category": null, "meaning": "Success."},
    {"code": 1, "category": "internal", "meaning": "Internal error."},
    {"code": 2, "category": "usage", "meaning": "Invalid usage / arguments."},
    {"code": 3, "category": "catalog_not_found", "meaning": "Catalog not found."},
    {"code": 4, "category": "catalog_already_exists", "meaning": "Catalog already exists."},
    {"code": 5, "category": "manifest_invalid", "meaning": "Catalog manifest (tome-catalog.toml) invalid."},
    {"code": 6, "category": "git_failed", "meaning": "A git operation failed."},
    {"code": 7, "category": "io", "meaning": "I/O error."},
    {"code": 8, "category": "interrupted", "meaning": "Interrupted (SIGINT / Ctrl-C)."},
    {"code": 9, "category": "plugin_data_dir_write_failed", "meaning": "Failed to write a plugin's data directory."},
    {"code": 13, "category": "workspace_not_found", "meaning": "Workspace not found."},
    {"code": 14, "category": "workspace_already_exists", "meaning": "Workspace already exists."},
    {"code": 15, "category": "workspace_name_invalid", "meaning": "Invalid workspace name."},
    {"code": 16, "category": "workspace_has_bound_projects", "meaning": "Workspace still has bound projects."},
    {"code": 17, "category": "composition_error", "meaning": "Workspace composition error."},
    {"code": 18, "category": "harness_not_supported", "meaning": "Unsupported harness."},
    {"code": 19, "category": "harness_clash", "meaning": "Harness configuration clash."},
    {"code": 20, "category": "plugin_not_found", "meaning": "Plugin not found."},
    {"code": 21, "category": "plugin_already_in_state", "meaning": "Plugin already in the requested state."},
    {"code": 22, "category": "plugin_manifest_parse_error", "meaning": "Plugin manifest (tome-plugin.toml) parse error."},
    {"code": 23, "category": "skill_frontmatter_parse_error", "meaning": "SKILL.md frontmatter parse error."},
    {"code": 24, "category": "summariser_failure", "meaning": "Summariser failure."},
    {"code": 25, "category": "workspace_data_dir_write_failed", "meaning": "Failed to write a workspace's data directory."},
    {"code": 26, "category": "prompt_argument_mismatch", "meaning": "MCP prompt argument mismatch."},
    {"code": 27, "category": "entry_not_found", "meaning": "Entry not found."},
    {"code": 28, "category": "substitution_failed", "meaning": "Variable substitution failed."},
    {"code": 29, "category": "invalid_argument_frontmatter", "meaning": "Invalid argument frontmatter."},
    {"code": 30, "category": "model_missing", "meaning": "A required model is missing."},
    {"code": 31, "category": "model_corrupt", "meaning": "A model file is corrupt."},
    {"code": 32, "category": "model_checksum_mismatch", "meaning": "Model checksum mismatch."},
    {"code": 33, "category": "model_registration_parse_error", "meaning": "Model registration parse error."},
    {"code": 34, "category": "inference_runtime_init_failure", "meaning": "Inference runtime failed to initialise."},
    {"code": 35, "category": "vector_extension_init_failure", "meaning": "Vector extension failed to initialise."},
    {"code": 36, "category": "embedding_generation_failure", "meaning": "Embedding generation failed."},
    {"code": 37, "category": "reranking_failure", "meaning": "Reranking failed."},
    {"code": 40, "category": "query_no_results_strict", "meaning": "--strict query returned no results."},
    {"code": 41, "category": "embedder_name_drift", "meaning": "Embedder name drift (index vs. configured model)."},
    {"code": 42, "category": "embedder_version_drift", "meaning": "Embedder version drift."},
    {"code": 43, "category": "hook_spec_parse_error", "meaning": "Hook spec parse error."},
    {"code": 44, "category": "hook_settings_write_failed", "meaning": "Failed to write hook settings."},
    {"code": 45, "category": "agent_translation_failed", "meaning": "Agent translation failed."},
    {"code": 46, "category": "guardrails_write_failed", "meaning": "Failed to write the guardrails/rules file."},
    {"code": 50, "category": "index_busy", "meaning": "The index is locked by another process."},
    {"code": 51, "category": "index_integrity_check_failure", "meaning": "Index integrity check failed."},
    {"code": 52, "category": "schema_too_new", "meaning": "Index schema is newer than this binary supports."},
    {"code": 53, "category": "catalog_has_enabled_plugins", "meaning": "Catalog still has enabled plugins (use --force)."},
    {"code": 54, "category": "not_a_terminal", "meaning": "An interactive command was run without a terminal."},
    {"code": 60, "category": "mcp_startup", "meaning": "MCP server failed to start."},
    {"code": 61, "category": "mcp_io", "meaning": "MCP protocol I/O error."},
    {"code": 70, "category": "workspace_malformed", "meaning": "Workspace data on disk is malformed."},
    {"code": 73, "category": "schema_too_new", "meaning": "Workspace schema version too new."},
    {"code": 74, "category": "schema_migration", "meaning": "Schema migration failed."},
    {"code": 75, "category": "doctor_fix_unsafe", "meaning": "A doctor --fix repair was not safe to apply."},
    {"code": 80, "category": "plugin_not_converted", "meaning": "Plugin not converted: legacy .claude-plugin/plugin.json exists but no tome-plugin.toml."},
    {"code": 81, "category": "output_exists", "meaning": "Refusing to overwrite existing output (pass --force)."},
    {"code": 82, "category": "template_invalid", "meaning": "Template unusable (missing file, malformed template, render error)."},
    {"code": 83, "category": "source_format_unrecognized", "meaning": "Could not auto-detect source format (pass --from <harness>)."},
    {"code": 84, "category": "conversion_unsupported_strict", "meaning": "convert --strict hit an unsupported feature."},
    {"code": 85, "category": "validation_found_errors", "meaning": "lint found at least one error."},
    {"code": 86, "category": "validation_strict_warnings", "meaning": "lint --strict found warnings (and no errors)."},
    {"code": 87, "category": "meta_skill_not_found", "meaning": "Unknown bundled meta skill id."},
    {"code": 88, "category": "meta_install_failed", "meaning": "Failed to install a meta skill."},
    {"code": 89, "category": "no_harness_detected", "meaning": "No supported harness detected (use --harness or install one)."}
  ]
}
```

- [ ] **Step 2: Write the generator**

Create `scripts/generate-cli-surface.mjs`:

```js
#!/usr/bin/env node
// Regenerates the `commands` half of specs/reference/cli-surface.json by
// recursively parsing `--help` from a locally built tome binary. The
// `exitCodes` half is hand-curated (it lives in tome's src/error.rs) and is
// preserved verbatim.
//
// Usage: node scripts/generate-cli-surface.mjs [path-to-tome-binary]
import {execFileSync} from 'node:child_process';
import {readFileSync, writeFileSync} from 'node:fs';
import {fileURLToPath} from 'node:url';
import path from 'node:path';

const root = path.dirname(path.dirname(fileURLToPath(import.meta.url)));
const contractPath = path.join(root, 'specs/reference/cli-surface.json');
const bin = process.argv[2] ?? path.join(root, '../tome/target/release/tome');

const IGNORED_FLAGS = new Set(['--help', '--version', '--json', '--workspace']);
const IGNORED_SUBCOMMANDS = new Set(['help']);

function help(args) {
  return execFileSync(bin, [...args, '--help'], {encoding: 'utf8'});
}

// clap v4 help: sections are "Usage:", "Commands:", "Options:"; entries are
// indented and the first token is the name.
function parseSection(text, section) {
  const lines = text.split('\n');
  const start = lines.findIndex((l) => l.trim() === `${section}:`);
  if (start === -1) return [];
  const out = [];
  for (let i = start + 1; i < lines.length; i++) {
    const line = lines[i];
    if (/^\S/.test(line)) break; // next section
    const m = line.match(/^\s{2,}(\S+)/);
    if (m) out.push({name: m[1], line});
  }
  return out;
}

function flagsOf(text) {
  const found = new Set();
  for (const {line} of parseSection(text, 'Options')) {
    for (const m of line.matchAll(/--[a-z][a-z0-9-]*/g)) {
      if (!IGNORED_FLAGS.has(m[0])) found.add(m[0]);
    }
  }
  return [...found].sort();
}

const top = help([]);
const commands = {};
for (const {name: group} of parseSection(top, 'Commands')) {
  if (IGNORED_SUBCOMMANDS.has(group)) continue;
  const groupHelp = help([group]);
  const subcommands = {};
  for (const {name: sub} of parseSection(groupHelp, 'Commands')) {
    if (IGNORED_SUBCOMMANDS.has(sub)) continue;
    subcommands[sub] = flagsOf(help([group, sub]));
  }
  commands[group] = {subcommands, flags: flagsOf(groupHelp)};
}

const existing = JSON.parse(readFileSync(contractPath, 'utf8'));
const next = {...existing, commands};
writeFileSync(contractPath, `${JSON.stringify(next, null, 2)}\n`);
console.log(`Regenerated ${Object.keys(commands).length} command groups into ${path.relative(root, contractPath)}`);
console.log('Exit codes preserved (hand-curated — source: tome/src/error.rs).');
```

- [ ] **Step 3: Write the checker**

Create `scripts/check-doc-drift.mjs`:

```js
#!/usr/bin/env node
// Fails CI when the docs and specs/reference/cli-surface.json disagree, in
// either direction:
//   forward: everything in the contract must appear in the docs
//   reverse: every exit code documented / `tome X` heading must be in the contract
import {readFileSync} from 'node:fs';
import {fileURLToPath} from 'node:url';
import path from 'node:path';

const root = path.dirname(path.dirname(fileURLToPath(import.meta.url)));
const contract = JSON.parse(readFileSync(path.join(root, 'specs/reference/cli-surface.json'), 'utf8'));
const commandsDoc = readFileSync(path.join(root, 'docs/reference/commands.md'), 'utf8');
const exitCodesDoc = readFileSync(path.join(root, 'docs/reference/exit-codes.md'), 'utf8');

const errors = [];

// -- forward: contract ⊆ docs ------------------------------------------------
for (const [group, def] of Object.entries(contract.commands)) {
  if (!commandsDoc.includes(`## \`tome ${group}\``)) {
    errors.push(`commands.md: missing section heading \`tome ${group}\``);
    continue;
  }
  for (const [sub, flags] of Object.entries(def.subcommands)) {
    if (!commandsDoc.includes(`\`${sub}`)) {
      errors.push(`commands.md: \`tome ${group}\` is missing subcommand \`${sub}\``);
    }
    for (const flag of flags) {
      if (!commandsDoc.includes(flag)) {
        errors.push(`commands.md: \`tome ${group} ${sub}\` is missing flag ${flag}`);
      }
    }
  }
  for (const flag of def.flags) {
    if (!commandsDoc.includes(flag)) {
      errors.push(`commands.md: \`tome ${group}\` is missing flag ${flag}`);
    }
  }
}
for (const flag of contract.globalFlags) {
  if (!commandsDoc.includes(flag)) errors.push(`commands.md: missing global flag ${flag}`);
}
for (const {code, category} of contract.exitCodes) {
  if (!exitCodesDoc.includes(`| \`${code}\` |`)) {
    errors.push(`exit-codes.md: missing row for code ${code}`);
  } else if (category && !exitCodesDoc.includes(`\`${category}\``)) {
    errors.push(`exit-codes.md: code ${code} present but category \`${category}\` missing`);
  }
}

// -- reverse: docs ⊆ contract --------------------------------------------------
const knownCodes = new Set(contract.exitCodes.map((e) => e.code));
for (const m of exitCodesDoc.matchAll(/^\| `(\d+)` \|/gm)) {
  const code = Number(m[1]);
  if (!knownCodes.has(code)) errors.push(`exit-codes.md: documents code ${code}, not in contract`);
}
for (const m of commandsDoc.matchAll(/^## `tome ([a-z-]+)`/gm)) {
  if (!(m[1] in contract.commands)) errors.push(`commands.md: documents \`tome ${m[1]}\`, not in contract`);
}

if (errors.length) {
  console.error(`DOC DRIFT — ${errors.length} problem(s):\n`);
  for (const e of errors) console.error(`  ✗ ${e}`);
  console.error('\nFix the docs, or regenerate the contract: node scripts/generate-cli-surface.mjs');
  process.exit(1);
}
console.log('✓ docs match specs/reference/cli-surface.json');
```

- [ ] **Step 4: Wire package.json scripts**

In `package.json`, add to `"scripts"` (after `"audit:security"`):

```json
    "drift:check": "node scripts/check-doc-drift.mjs",
    "drift:generate": "node scripts/generate-cli-surface.mjs",
```

- [ ] **Step 5: Run the checker — verify it FAILS against current docs**

```bash
pnpm drift:check; echo "exit: $?"
```

Expected: FAIL (exit 1) with `ENOENT … docs/reference/commands.md` — the doc lives at `docs/commands/reference.md` today. Move it now: `git mv docs/commands/reference.md docs/reference/commands.md` (note: `sidebars.ts` still points at `commands/reference`, so `pnpm build` is intentionally broken from here until the Task 11 IA flip). Re-run `pnpm drift:check`; expected output includes at minimum:
- `commands.md: missing section heading \`tome skill\``
- `commands.md: missing section heading \`tome meta\``
- `commands.md: \`tome catalog\` is missing subcommand \`create\`` (and `convert`, `lint`)
- `exit-codes.md: missing row for code 80` … through `89`

- [ ] **Step 6: Commit**

```bash
git add specs/reference/cli-surface.json scripts/generate-cli-surface.mjs scripts/check-doc-drift.mjs package.json docs
git commit -m "feat(site): doc-drift contract, generator, and checker (failing against current docs)"
```

---

### Task 3: Regenerate the contract + capture real CLI sessions

Everything quoted in docs and the landing demo comes from these captures.

**Files:**
- Modify: `specs/reference/cli-surface.json` (regenerated command half)
- Create: `specs/reference/captures/*.txt` (one file per session)

- [ ] **Step 1: Build/verify the tome binary**

```bash
cd /Users/aaronbassett/Projects/devrel-ai/tome && cargo build --release \
  && mkdir -p /tmp/tome-bin-cache && cp target/release/tome /tmp/tome-bin-cache/tome \
  && /tmp/tome-bin-cache/tome --version
```

Expected: prints `tome 0.6.0` (or current version). If the build fails, stop and report — do not proceed with a stale binary. **Use `/tmp/tome-bin-cache/tome` as `$B` for every later step** — an external cache cleaner has been observed wiping `tome/target/` mid-session (2026-06-10, twice).

- [ ] **Step 2: Regenerate the contract's command half**

```bash
cd /Users/aaronbassett/Projects/devrel-ai/tome-site && pnpm drift:generate && git diff specs/reference/cli-surface.json
```

Expected: "Regenerated N command groups". Review the diff: if the regenerated surface differs from the Task 2 seed (extra/missing flags or subcommands), the binary wins. If a whole expected group (e.g. `meta`) is missing, stop and report.

- [ ] **Step 3: Convert the real midnight-expert marketplace locally**

`devrelaicom/midnight-expert-tome` is NOT published yet (verified 404, 2026-06-10 — recorded as a launch hard-stop in spec §11). The honest source is the real Claude Code marketplace clone on this machine. Convert it, make the output a git repo, and add it by local path (`catalog add` interprets a local path as `file://`, so the converted catalog must be a committed git repo):

```bash
B=/tmp/tome-bin-cache/tome
mkdir -p specs/reference/captures
SRC=~/.claude/plugins/marketplaces/midnight-expert
rm -rf /tmp/me-tome-catalog
$B catalog convert "$SRC" --output /tmp/me-tome-catalog --dry-run > specs/reference/captures/convert-dry-run.txt 2>&1 || true
$B catalog convert "$SRC" --output /tmp/me-tome-catalog           > specs/reference/captures/convert.txt 2>&1
$B catalog lint /tmp/me-tome-catalog                              > specs/reference/captures/lint.txt 2>&1 || true
git -C /tmp/me-tome-catalog init -b main -q && git -C /tmp/me-tome-catalog add -A \
  && git -C /tmp/me-tome-catalog -c user.email=cap@local -c user.name=capture commit -qm "converted catalog"
```

If the convert itself errors (not warns — warnings are good documentation), stop and report. Record `# source: devrelaicom/midnight-expert (local marketplace clone)` as the first line of each capture file.

- [ ] **Step 4: Capture the real sessions against the converted catalog**

```bash
B=/tmp/tome-bin-cache/tome
$B models list                                            > specs/reference/captures/models-list.txt 2>&1
$B models download                                                                  # fresh machine: required before query (~325 MB)
$B catalog add /tmp/me-tome-catalog --name midnight-expert > specs/reference/captures/catalog-add.txt 2>&1
$B catalog show midnight-expert                           > specs/reference/captures/catalog-show.txt 2>&1
# pick ONE real plugin name from catalog-show.txt for the canonical examples:
$B plugin enable <real-plugin-from-show>                  > specs/reference/captures/plugin-enable.txt 2>&1 || true
$B plugin list                                            > specs/reference/captures/plugin-list.txt 2>&1
$B query "verify a Compact contract"                      > specs/reference/captures/query.txt 2>&1
$B query "verify a Compact contract" --top-k 3            > specs/reference/captures/query-topk.txt 2>&1
$B meta list                                              > specs/reference/captures/meta-list.txt 2>&1
$B status                                                 > specs/reference/captures/status.txt 2>&1
$B doctor                                                 > specs/reference/captures/doctor.txt 2>&1 || true
$B harness list                                           > specs/reference/captures/harness-list.txt 2>&1
$B skill create demo-skill --bare --output /tmp/tome-cap  > specs/reference/captures/skill-create.txt 2>&1 || true
ls /tmp/tome-cap                                          >> specs/reference/captures/skill-create.txt 2>&1
find /tmp/me-tome-catalog -name tome-plugin.toml | head -1 | xargs cat > specs/reference/captures/tome-plugin-toml.txt 2>&1
```

If `catalog add` of the local path fails after the git-init, stop and report with the exact error.

- [ ] **Step 5: Extract the headline numbers**

Read `catalog-show.txt` and `query.txt`; write `specs/reference/captures/NUMBERS.md` recording: plugin count, entry count (skills/commands/agents/hooks), the top query hits with scores, and the canonical demo plugin name chosen in Step 4. These are the only numbers/names the landing page and docs may use. **Slug rule:** docs and the landing demo show the intended public slug `devrelaicom/midnight-expert-tome` in `catalog add` command lines (spec §11 records the publish dependency), but may only quote count/score lines from these captures — never a line embedding the local `/tmp` path.

- [ ] **Step 6: Commit**

```bash
git add specs/reference
git commit -m "docs(site): regenerate CLI contract from binary; capture real CLI sessions"
```

---

### Task 4: Reference — `commands.md` + `exit-codes.md` (drift goes green)

**Files:**
- Modify: `docs/reference/commands.md` (moved in Task 2; full rewrite)
- Modify: `docs/reference/exit-codes.md` (complete table)

- [ ] **Step 1: Rewrite `docs/reference/commands.md`**

Frontmatter: `title: Commands`, `sidebar_position: 1`. Lede (reference voice — one wry line, then business): "Every command, subcommand, and flag — the complete index of the spellbook. Tome exits `0` on success and a specific non-zero code for every failure class; see [Exit codes](./exit-codes.md)."

Required content — one `## \`tome <group>\`` section per contract group, in this order: `catalog`, `plugin`, `skill`, `query`, `models`, `reindex`, `status`, `doctor`, `workspace`, `harness`, `meta`, `mcp`. Each section: one-sentence purpose + a table of subcommands (or flag list for leaf commands). **Every subcommand and every flag in `specs/reference/cli-surface.json` must appear** (the drift checker enforces this literally). New-in-this-rewrite content that must be present:

- `catalog`/`plugin`/`skill` sections each document `create`, `convert`, `lint` rows with their flags (`--template`, `--output`, `--into`, `--force`; `--name`, `--from`, `--dry-run`, `--strict`, `--no-fetch` (catalog convert only); `--autofix`; skill create additionally `--bare`, `--plugin-name`). Cross-link each to the Authoring chapter pages (`../authoring/create.md`, `../authoring/convert.md`, `../authoring/lint.md`).
- `query` documents all of `--top-k`, `--catalog`, `--plugin`, `--no-rerank`, `--strict`, `--min-score`.
- `meta` section: `list`, `add <skill_id>` (`--harness` repeatable, `--global`, `--force`), `remove <skill_id>` (`--harness`, `--global`); cross-link `../using-tome/meta-skills.md`.
- `mcp` documents `--harness` (host-harness stamping, written by `harness sync` — cross-link `../using-tome/mcp-server.md`).
- Keep the existing accurate material (interactive bare `tome plugin` / `tome harness`, `reindex` scopes, `status` never takes the index lock, `doctor` read-only by default).
- `## Global behaviour` section: `--json` on read-only inspection commands (orthogonal to stderr logging), `--workspace <name>`, `-v`/`-vv`, SIGINT → exit 8.

- [ ] **Step 2: Complete `docs/reference/exit-codes.md`**

Keep the existing intro and table rows 0–75 (fix row 22's meaning to "Plugin manifest (`tome-plugin.toml`) parse error." — the `plugin.json` wording is stale post-cutover). Delete the `TODO(launch)` comment line (the drift checker replaces it — note that in the intro: "This table is enforced against the CLI contract in CI."). Append rows 80–89 exactly as in the contract (code, category, meaning). Add a short `## Authoring verdicts` aside after the table: codes 85/86 are *verdicts* from `lint` (found-problems vs. crash) — scripts and CI should branch on them; cross-link `../authoring/lint.md`.

- [ ] **Step 3: Run the drift check — verify it PASSES**

```bash
pnpm drift:check
```

Expected: `✓ docs match specs/reference/cli-surface.json`. If it fails, the error list is the remaining to-do — fix the docs (or, for a generator artifact that's genuinely wrong, fix the generator) until green.

- [ ] **Step 4: Commit**

```bash
git add docs/reference/commands.md docs/reference/exit-codes.md
git commit -m "docs(site): complete command + exit-code reference (Phase 8/9 surface); drift check green"
```

---

### Task 5: Reference — `config.md` + `security-model.md`

**Files:**
- Modify: `docs/reference/config.md`
- Modify: `docs/reference/security-model.md`

- [ ] **Step 1: Rewrite `config.md`** (audit found it accurate — this is a voice/depth pass, same facts). Keep `~/.tome/` layout claims exactly as currently documented. Add a lede; add a worked `tree ~/.tome` style block ONLY if present in a capture; otherwise keep the existing descriptive list.

- [ ] **Step 2: Expand `security-model.md`** (currently 28 lines). Concept-page skeleton — the problem (you are executing third-party instructions inside your agent), then the boundary. Must cover, in plain language: catalogs-as-trust-decisions (the npm-install analogy, kept from the current page); what Tome *does* defend: symlink-refusing writes into harness config, path validation on third-party names, bounded reads of third-party files, credential scrubbing in git/network errors, everything-local (no telemetry); what Tome does *not* defend: the content of skills you choose to enable. Close with the trust note for authors (link `../authoring/distributing.md`). Do not invent mechanisms beyond these — each listed defence exists in the CLI (per the 2026-06-10 audit + tome CLAUDE.md §patterns).

- [ ] **Step 3: Commit**

```bash
git add docs/reference/config.md docs/reference/security-model.md
git commit -m "docs(site): config + security-model rewrite (reading-room voice)"
```

---

### Task 6: Getting started — `install.md`, `quickstart.md`, `concepts.md`

**Files:**
- Modify: `docs/getting-started/install.md`
- Modify: `docs/getting-started/quickstart.md`
- Modify: `docs/getting-started/concepts.md`

- [ ] **Step 1: `install.md`** — facts unchanged (audit-verified; brew/cargo/source instructions stay — the launch dependency is recorded in spec §11). Voice pass: lede on "one binary, batteries included" (models download on demand; index/search built in). Keep the platform table and prerequisites verbatim — they are correct.

- [ ] **Step 2: `quickstart.md`** — keep the 4-command structure (it's good); upgrade each step with the real captured output from `specs/reference/captures/` (`catalog-add.txt`, `plugin-enable.txt`, `query.txt`) so the reader sees what success looks like. After step 4, add a 2-line "What just happened" aside: the query ran KNN + rerank locally; in a harness the same search runs over MCP and the agent loads only the winner. Update "Next steps" links for the new IA (`../using-tome/plugins-and-catalogs.md`, `../reference/commands.md`).

- [ ] **Step 3: `concepts.md`** — keep the six-term structure (Catalog, Plugin, Entry kinds, Workspace, Harness, Central index, MCP server). Fixes: plugin definition now says "described by a `tome-plugin.toml` manifest" with one sentence on the cutover ("Plugins that only have a legacy Claude Code `plugin.json` aren't loaded — `tome plugin convert` migrates them; unconverted plugins exit `80`."). Add a 7th term: **Meta skill** (a Tome-bundled skill that teaches an agent to use Tome itself; link `../using-tome/meta-skills.md`). Add one diagram (Mermaid or simple ASCII-in-code-block): catalog → plugins → entries → index → harnesses/MCP flow.

- [ ] **Step 4: Commit**

```bash
git add docs/getting-started
git commit -m "docs(site): getting-started rewrite — real output, manifest cutover, meta skills"
```

---

### Task 7: Using Tome (1/2) — `plugins-and-catalogs.md` (new), `search.md`, `harnesses.md`

**Files:**
- Create: `docs/using-tome/plugins-and-catalogs.md`
- Modify: `docs/using-tome/search.md`
- Modify: `docs/using-tome/harnesses.md`

- [ ] **Step 1: Create `plugins-and-catalogs.md`** — frontmatter `title: Plugins & catalogs`, `sidebar_position: 1`. Task-page skeleton:
  - Lede: the bookshelf — catalogs are volumes you add once; plugins are the chapters you choose to read from.
  - The day-to-day lifecycle with captured output: `catalog add` / `update` / `remove` (`--force` cascades), `plugin enable`/`disable`/`list`/`show`, bare `tome plugin` interactive picker.
  - `## Catalogs to add today` — the midnight-expert showcase absorbed from the deleted page: keep its description + install commands, replace the TODO placeholders with the real counts from `captures/NUMBERS.md`.
  - Pitfalls: exit 4 (already added), 21 (already enabled), 53 (remove with enabled plugins → `--force`), 80 (unconverted plugin → link to authoring/convert).

- [ ] **Step 2: `search.md`** — add the missing flags (`--top-k`, `--no-rerank`, `--min-score`) with one-line semantics each; worked example from `captures/query.txt` + `query-topk.txt`; keep the existing KNN + reranker explanation; pitfall: `--strict` + no results → exit 40.

- [ ] **Step 3: `harnesses.md`** — content is audit-verified; this is a voice/depth pass. New lede = the approved §5 calibration sample ("You probably don't use one coding agent…"). Keep the per-harness table and caveats verbatim (they are correct, including Gemini's no-native-agents). Add captured `harness list` output. Add one sentence under Claude Code/the MCP row: `harness sync` stamps the host into the spawned server args (`tome mcp --workspace <ws> --harness <name>`), which the MCP `meta` tool relies on.

- [ ] **Step 4: Commit**

```bash
git add docs/using-tome
git commit -m "docs(site): plugins-and-catalogs page; search flags; harnesses voice pass"
```

---

### Task 8: Using Tome (2/2) — `meta-skills.md` (new), `mcp-server.md`, `workspaces.md`, `troubleshooting.md`

**Files:**
- Create: `docs/using-tome/meta-skills.md`
- Modify: `docs/using-tome/mcp-server.md`
- Modify: `docs/using-tome/workspaces.md`
- Modify: `docs/using-tome/troubleshooting.md`

- [ ] **Step 1: Create `meta-skills.md`** — `title: Meta skills`. Task-page skeleton:
  - Lede: skills that teach your agent to use Tome itself — bundled inside the binary, installed as native `SKILL.md` files into your harness.
  - `tome meta list` with captured output; `tome meta add <skill_id>` (default: project scope, all detected harnesses; `--harness <name>` repeatable to choose; `--global` for the harness's global skill dir; `--force` to overwrite); `tome meta remove`.
  - The first bundled skill: `convert-marketplace` — a guided Claude-Code-marketplace→Tome conversion that drives `tome catalog convert` + `tome catalog lint` and reports for confirmation before registering anything.
  - Drift: `tome doctor` reports installed-meta-skill drift (missing/stale/modified); `doctor --fix` re-installs.
  - Pitfalls: exit 87 (unknown skill id), 88 (install failed), 89 (no harness detected — pass `--harness`).
  - Note: Gemini CLI does not support native skills, so it is not a meta-skill target.

- [ ] **Step 2: `mcp-server.md`** — keep the three-tool search-then-load flow; add the fourth tool: **`meta`** — installs a bundled meta skill into the *host* harness (install-only; the host identity comes from the `--harness` flag that `tome harness sync` stamps into the server args; with no/unknown host the tool fails closed with the `no_harness_detected` category rather than guessing). Update the Tools intro from "three tools" to "four". Prompts section: add that Tome also registers one built-in always-on prompt, `add-tome-conversion-skill`, which installs the conversion meta skill; plugin prompts never displace it (collisions get suffixed).

- [ ] **Step 3: `workspaces.md`** — voice/depth pass, facts unchanged (audit-verified). Lede on per-project composition; worked example: two workspaces with different plugin sets; captured `workspace info` if available, otherwise command blocks without fabricated output.

- [ ] **Step 4: `troubleshooting.md`** — voice pass + add a "common failures" table mapping symptoms → exit code → fix, including the new ones: 80 (run `tome plugin convert`), 83 (pass `--from`), 89 (pass `--harness`). Keep `status` (read-only, never locks) vs `doctor` (`--fix` repairs) framing. Captured `status.txt` / `doctor.txt` excerpts.

- [ ] **Step 5: Commit**

```bash
git add docs/using-tome
git commit -m "docs(site): meta-skills page; MCP meta tool + reserved prompt; troubleshooting exit-code table"
```

---

### Task 9: Authoring (1/2) — `overview.md`, `create.md`, `distributing.md`

**Files:**
- Create: `docs/authoring/overview.md`
- Create: `docs/authoring/create.md`
- Create: `docs/authoring/distributing.md` (content evolved from `docs/catalogs/authoring.md`; the old file is deleted in Task 11)

- [ ] **Step 1: Create `overview.md`** — `title: Authoring overview`, `sidebar_position: 1`. Concept page:
  - Lede: Ch. III framing — "in which you write your own".
  - Anatomy of a catalog (adapted from the current `catalogs/authoring.md` tree diagram) **with the manifest corrected**: `tome-plugin.toml` at the plugin root is the manifest Tome reads (strict — unknown fields rejected); `.claude-plugin/plugin.json` appears in the tree only as "legacy (converted by `tome plugin convert`)".
  - `tome-catalog.toml` example (keep the existing TOML block — it is correct).
  - A `tome-plugin.toml` example: derive the exact field set from the Task 3 `skill-create` capture (`cat /tmp/tome-cap/*/tome-plugin.toml` — add that to the capture if missing). Do not guess fields.
  - Entry kinds from the author's view (skills + `when_to_use` indexing, commands, agents, hooks — keep current accurate text).
  - The three verbs, one paragraph: `create` (scaffold, lint-clean by construction) / `convert` (migrate) / `lint` (validate) — each linking to its page.

- [ ] **Step 2: Create `create.md`** — `title: Creating`, task page: scaffold a skill (`tome skill create my-skill --bare`), a plugin (`tome plugin create`), a catalog (`tome catalog create`); captured output from `skill-create.txt` incl. the resulting tree; flag semantics: `--bare` (naked `<NAME>/SKILL.md`, no plugin wrapper) vs `--plugin-name` (wrap) vs `--into` (inject into existing plugin) vs `--output` (parent dir; `--into`/`--output` mutually exclusive), `--template` (built-ins only today — a non-builtin source exits 82), `--force` (overwrite, exit 81 otherwise). Pitfalls: 81, 82.

- [ ] **Step 3: Create `distributing.md`** — `title: Distributing`, evolved from the old page: publish-to-git host flow, `tome catalog add <your/repo>` consumer command, the trust note (keep, link to security-model). Add: run `tome catalog lint --strict` in your CI before tagging (link lint page); recommend committing `tome-plugin.toml` (converted) rather than legacy manifests so consumers don't hit exit 80.

- [ ] **Step 4: Commit**

```bash
git add docs/authoring
git commit -m "docs(site): authoring track — overview, create, distributing"
```

---

### Task 10: Authoring (2/2) — `convert.md`, `lint.md`

**Files:**
- Create: `docs/authoring/convert.md`
- Create: `docs/authoring/lint.md`

- [ ] **Step 1: Create `convert.md`** — `title: Converting`, the headline authoring page. Task skeleton:
  - Lede: you already have plugins — a Claude Code marketplace, a Cursor rules folder, a pile of `SKILL.md`s. Convert, don't rewrite.
  - What converts: Claude Code marketplaces / plugins / skills; Codex projects; native `SKILL.md` from Cursor, OpenCode, Cline, generic agent-skills. (Source list must match the `--from` values in the contract: `claude-code|codex|cursor|opencode|cline|agent-skills`.)
  - Worked example from `captures/convert-dry-run.txt` (+ the real `lint.txt` after-pass): `tome catalog convert <source> --output <dir> --dry-run` → review → run without `--dry-run`.
  - Mechanics that must be stated: auto-detection with `--from` override (detection failure → exit 83); local paths win over remote; remote sources are shallow-cloned to a temp dir and cleaned up; `--into` registers into an existing catalog; honest warn-on-unsupported (hooks-monitors/LSP/themes/output-styles and friends are *reported*, not silently dropped) and `--strict` turning unsupported into failure (exit 84); harness-isms rewritten (`${CLAUDE_*}` → `${TOME_*}`, legacy `$1..$9` → 0-based args).
  - The cutover story: why exit 80 exists; converting in place vs `--output`.
  - The agent-assisted path: `tome meta add convert-marketplace` gives your agent the guided version (link meta-skills page).

- [ ] **Step 2: Create `lint.md`** — `title: Linting`, task page: `tome {catalog,plugin,skill} lint <path>`; never halts on a malformed artifact — malformed input is a *finding*, not a crash; `--autofix` applies safe fixes to a fixpoint; `--dry-run` reports without writing; `--strict` promotes warnings to failure. CI recipe block:

```yaml
- run: tome catalog lint . --strict
  # exit 85 = errors found, 86 = warnings under --strict, 0 = clean
```

  Verdict semantics: 85/86 are verdicts (link exit-codes). Captured `lint.txt` excerpt.

- [ ] **Step 3: Commit**

```bash
git add docs/authoring
git commit -m "docs(site): authoring track — convert (headline) and lint"
```

---

### Task 11: The IA flip — sidebars, deletions, blog removal, chrome links

After this task the build must be green and stay green.

**Files:**
- Modify: `sidebars.ts` (full rewrite)
- Modify: `docusaurus.config.ts` (blog off, navbar, tagline)
- Modify: `src/theme/Footer/index.tsx` (links)
- Delete: `docs/catalogs/` (both files), `docs/commands/` (now empty), `blog/` (entire dir)

- [ ] **Step 1: Rewrite `sidebars.ts`**

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docs: [
    {type: 'category', label: 'Ch. I — Getting started', collapsed: false, items: [
      'getting-started/install', 'getting-started/quickstart', 'getting-started/concepts']},
    {type: 'category', label: 'Ch. II — Using Tome', collapsed: false, items: [
      'using-tome/plugins-and-catalogs', 'using-tome/search', 'using-tome/harnesses',
      'using-tome/workspaces', 'using-tome/meta-skills', 'using-tome/mcp-server',
      'using-tome/troubleshooting']},
    {type: 'category', label: 'Ch. III — Authoring', collapsed: false, items: [
      'authoring/overview', 'authoring/create', 'authoring/convert', 'authoring/lint',
      'authoring/distributing']},
    {type: 'category', label: 'Ch. IV — Reference', items: [
      'reference/commands', 'reference/exit-codes', 'reference/config',
      'reference/security-model']},
  ],
};

export default sidebars;
```

- [ ] **Step 2: Delete the dead content**

```bash
git rm -r docs/catalogs blog
rmdir docs/commands 2>/dev/null || true   # emptied by the Task 2 git mv; nothing tracked remains
```

(`docs/catalogs/authoring.md`'s surviving content was already evolved into `docs/authoring/` in Task 9; `midnight-expert.md` was absorbed in Task 7.)

- [ ] **Step 3: Update `docusaurus.config.ts`**

- In the preset options, replace the whole `blog: {…}` object with `blog: false,`.
- `tagline`: `'One bookshelf, every coding agent.'`
- Navbar `items` (spec §5 — minimal):

```ts
items: [
  {to: '/docs/getting-started/install', label: 'Docs', position: 'left'},
  {href: 'https://github.com/devrelaicom/tome', label: 'GitHub', position: 'right'},
  {href: 'https://github.com/devrelaicom/tome/releases', label: 'v0.6.0', position: 'right', className: 'navbar-version-badge'},
],
```

- [ ] **Step 4: Update footer links** in `src/theme/Footer/index.tsx` `COLS`: Product → Install `/docs/getting-started/install`, Quickstart `/docs/getting-started/quickstart`, Authoring `/docs/authoring/overview`, Changelog (unchanged). Project → GitHub (unchanged), Security `/docs/reference/security-model`, Exit codes `/docs/reference/exit-codes` (Blog link deleted). Midnight column unchanged. Change the `bar` right-hand span text from `FIG. 001 — built with care, on paper` to `EX LIBRIS — kept on your machine`.

- [ ] **Step 5: Full build — the link gate**

```bash
pnpm typecheck && pnpm build
```

Expected: PASS with zero broken-link errors (`onBrokenLinks: 'throw'`). Every internal link added in Tasks 4–10 resolves or this fails — fix any reported link before committing.

- [ ] **Step 6: Run drift check again (paths unchanged but prove it)**

```bash
pnpm drift:check
```

Expected: `✓ docs match specs/reference/cli-surface.json`

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(site): four-chapter IA flip; blog removed; chrome links updated; build green"
```

---

### Task 12: Grimoire theme — tokens, fonts, reading-room docs

**Files:**
- Modify: `package.json` + lockfile (font swap)
- Modify: `src/css/custom.css` (full rewrite)
- Modify: `docusaurus.config.ts` (colorMode, prism)
- Modify: `src/theme/Footer/footer.module.css` (palette)

- [ ] **Step 1: Swap font packages**

```bash
sfw pnpm remove @fontsource/space-grotesk @fontsource/ibm-plex-mono
sfw pnpm add @fontsource-variable/newsreader
```

Expected: lockfile updated; Newsreader is the only font dependency (spec: the one downloaded font; body/mono use system stacks).

- [ ] **Step 2: Rewrite `src/css/custom.css`**

```css
/* Newsreader — the grimoire's display voice (self-hosted; the only webfont) */
@import '@fontsource-variable/newsreader';
@import '@fontsource-variable/newsreader/ital.css';

:root {
  /* parchment (light) — Tome grimoire tokens */
  --tome-paper: #f4ecdc;
  --tome-paper-deep: #ece1c8;
  --tome-ink: #1d1a14;
  --tome-ink-soft: #4a4337;
  --tome-oxblood: #8a2f1f;
  --tome-gold: #6b5d3f;
  --tome-cream: #f0e9d8;
  --tome-rule: #d4c5a0;
  --tome-code-bg: #1d1a14; /* code blocks stay dark slate in BOTH modes */

  --tome-serif: 'Newsreader Variable', Georgia, 'Times New Roman', serif;
  --tome-sans: system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif;
  --tome-mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;

  /* Infima */
  --ifm-color-primary: #8a2f1f;
  --ifm-color-primary-dark: #7c2a1c;
  --ifm-color-primary-darker: #752819;
  --ifm-color-primary-darkest: #602114;
  --ifm-color-primary-light: #983425;
  --ifm-color-primary-lighter: #a03727;
  --ifm-color-primary-lightest: #b03e2c;
  --ifm-background-color: var(--tome-paper);
  --ifm-font-color-base: var(--tome-ink);
  --ifm-font-family-base: var(--tome-sans);
  --ifm-font-family-monospace: var(--tome-mono);
  --ifm-heading-font-family: var(--tome-serif);
  --ifm-heading-font-weight: 700;
  --ifm-code-font-size: 90%;
  --ifm-navbar-background-color: rgba(244, 236, 220, 0.92);
  --ifm-toc-border-color: var(--tome-rule);
  --docusaurus-highlighted-code-line-bg: rgba(138, 47, 31, 0.18);
}

/* midnight library (dark) — candlelit, not gray-blue */
[data-theme='dark'] {
  --tome-paper: #171310;
  --tome-paper-deep: #100d0a;
  --tome-ink: #f0e9d8;
  --tome-ink-soft: #b5a98e;
  --tome-oxblood: #c45a40; /* warmed + brightened for AA on dark */
  --tome-gold: #9a8a5f;
  --tome-rule: #3a322a;

  --ifm-color-primary: #c45a40;
  --ifm-color-primary-dark: #b85138;
  --ifm-color-primary-darker: #ad4c34;
  --ifm-color-primary-darkest: #8f3f2b;
  --ifm-color-primary-light: #ca6850;
  --ifm-color-primary-lighter: #cf7159;
  --ifm-color-primary-lightest: #d98b76;
  --ifm-background-color: var(--tome-paper);
  --ifm-background-surface-color: var(--tome-paper-deep);
  --ifm-font-color-base: var(--tome-ink);
  --ifm-navbar-background-color: rgba(23, 19, 16, 0.92);
}

/* headings: serif with tight tracking; h1 gets the oxblood full stop via docs CSS, not content */
h1, h2, h3 { letter-spacing: -0.015em; }
article header h1 { font-size: 2.6rem; }

/* the reading room: measure + rhythm */
article { font-size: 16.5px; line-height: 1.65; }
article .markdown > p { max-width: 72ch; }

/* sidebar chapters */
.menu__list-item-collapsible .menu__link--sublist {
  font-family: var(--tome-serif);
  font-style: italic;
  font-weight: 600;
}
.menu__link--active:not(.menu__link--sublist) {
  border-left: 2px solid var(--tome-oxblood);
  border-radius: 0;
}

/* code blocks: dark slate in both modes */
:root .theme-code-block, [data-theme='dark'] .theme-code-block {
  --prism-background-color: var(--tome-code-bg);
  box-shadow: none;
  border: 1px solid var(--tome-rule);
}
:not(pre) > code {
  background: var(--tome-paper-deep);
  border: 1px solid var(--tome-rule);
  color: inherit;
}

/* navbar */
.navbar { border-bottom: 1px solid var(--tome-rule); backdrop-filter: blur(6px); }
.navbar__title { font-family: var(--tome-serif); font-weight: 700; font-size: 1.25rem; }
.navbar__link { font-family: var(--tome-mono); font-size: 13px; letter-spacing: .04em; }
.navbar-version-badge {
  border: 1px solid var(--tome-rule);
  border-radius: 999px;
  padding: 2px 12px;
  font-family: var(--tome-mono);
  font-size: 12px;
}
```

(Adjust selectors only if a Docusaurus class name differs at build time — verify with `pnpm start`, do not guess.)

- [ ] **Step 3: `docusaurus.config.ts` theme bits**

- `colorMode: {defaultMode: 'light', disableSwitch: false, respectPrefersColorScheme: true}`
- `prism: {theme: prismThemes.oneDark, darkTheme: prismThemes.oneDark}` (dark slate in both modes, per spec).

- [ ] **Step 4: Footer palette** — in `src/theme/Footer/footer.module.css`, replace color values with the token vars (`var(--tome-ink)` background in light / `var(--tome-paper-deep)` in dark, `var(--tome-cream)` text, `var(--tome-gold)` headings, oxblood link hover). Keep layout rules unchanged.

- [ ] **Step 5: Contrast check (spec §10 gate)**

For each pair, compute the WCAG ratio (e.g. https://webaim.org/resources/contrastchecker/ or any local tool): ink `#1d1a14` on paper `#f4ecdc`; oxblood `#8a2f1f` on paper (links/accents ≥ 4.5:1 required); cream `#f0e9d8` on `#171310`; `#c45a40` on `#171310`. Expected: all ≥ 4.5:1 for body-size text. If oxblood-on-paper or `#c45a40`-on-dark falls short, darken/lighten the failing token until it passes and record the final value in this plan file as a checked-off note.

- [ ] **Step 6: Visual sanity + build**

```bash
pnpm start   # eyeball: docs page in light + dark, sidebar chapters, code blocks
pnpm build
```

Expected: build PASS; both modes legible; toggle works.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(site): grimoire theme — parchment/midnight tokens, Newsreader, reading-room docs"
```

---

### Task 13: Landing page rebuild

**Files:**
- Create: `src/components/landing/TypedTerminal.tsx`
- Create: `src/components/landing/Sprawl.tsx`, `Method.tsx`, `Colophon.tsx`, `AuthoringOnramp.tsx`, `FinalCta.tsx`
- Modify: `src/components/landing/Hero.tsx` (full rewrite)
- Modify: `src/components/landing/landing.module.css` (full rewrite)
- Modify: `src/pages/index.tsx`
- Delete: `src/components/landing/{Highlights,HowItWorks,SpecSheet,FeaturedCatalog,GetStarted,AuthorOnramp}.tsx`, `static/img/tome-exploded.png`

- [ ] **Step 1: Write `TypedTerminal.tsx`**

```tsx
import React, {useEffect, useRef, useState} from 'react';
import styles from './landing.module.css';

export type TermLine = {kind: 'cmd' | 'out' | 'ok'; text: string};

// Auto-typing looping terminal. Honors prefers-reduced-motion (renders the
// full transcript statically) and only starts when scrolled into view.
export default function TypedTerminal({lines, label}: {lines: TermLine[]; label: string}): React.JSX.Element {
  const [shown, setShown] = useState<TermLine[]>([]);
  const [partial, setPartial] = useState('');
  const [reduced, setReduced] = useState(false);
  const [started, setStarted] = useState(false);
  const rootRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const mq = window.matchMedia('(prefers-reduced-motion: reduce)');
    setReduced(mq.matches);
    const onChange = (e: MediaQueryListEvent) => setReduced(e.matches);
    mq.addEventListener('change', onChange);
    return () => mq.removeEventListener('change', onChange);
  }, []);

  useEffect(() => {
    const el = rootRef.current;
    if (!el) return;
    const io = new IntersectionObserver(([entry]) => entry.isIntersecting && setStarted(true), {threshold: 0.4});
    io.observe(el);
    return () => io.disconnect();
  }, []);

  useEffect(() => {
    if (reduced || !started) return;
    let line = 0;
    let char = 0;
    let cancelled = false;
    const timers: number[] = [];
    const later = (fn: () => void, ms: number) => timers.push(window.setTimeout(fn, ms));
    const step = () => {
      if (cancelled) return;
      if (line >= lines.length) {
        later(() => { setShown([]); setPartial(''); line = 0; char = 0; step(); }, 3500);
        return;
      }
      const cur = lines[line];
      if (cur.kind === 'cmd' && char < cur.text.length) {
        char += 1;
        setPartial(cur.text.slice(0, char));
        later(step, 28);
      } else {
        setShown((prev) => [...prev, cur]);
        setPartial('');
        line += 1;
        char = 0;
        later(step, cur.kind === 'cmd' ? 350 : 650);
      }
    };
    step();
    return () => { cancelled = true; timers.forEach(clearTimeout); };
  }, [reduced, started, lines]);

  const render = (l: TermLine, i: number) => (
    <div key={i} className={styles[`term_${l.kind}`]}>{l.kind === 'cmd' ? <><span className={styles.termPrompt}>$ </span>{l.text}</> : l.text}</div>
  );

  return (
    <div ref={rootRef} className={styles.terminal} role="img" aria-label={label}>
      <div className={styles.termTitle}>{label}</div>
      {(reduced ? lines : shown).map(render)}
      {!reduced && partial && <div className={styles.term_cmd}><span className={styles.termPrompt}>$ </span>{partial}<span className={styles.termCursor} /></div>}
    </div>
  );
}
```

- [ ] **Step 2: Write the six sections + `index.tsx`**

All copy below is final unless it contains a `{NUMBERS}` reference — replace those from `specs/reference/captures/NUMBERS.md` (and make the demo lines match the real `catalog-add.txt` / `query.txt` shapes).

`Hero.tsx` (rewrite): kicker `CHAPTER I — IN WHICH YOUR AGENTS LEARN TO READ`; `<h1>Tome<span className={styles.stop}>.</span></h1>`; lede (italic serif): *“One bookshelf for everything your coding agents know — read everywhere, loaded one skill at a time.”*; CTAs: solid `▸ Install Tome` → `/docs/getting-started/install`, outline `Read the docs` → `/docs/getting-started/quickstart`; then `<TypedTerminal label="fig. 1 — the bookshelf at work" lines={…}/>` with the demo script (real numbers):

```ts
const DEMO: TermLine[] = [
  {kind: 'cmd', text: 'tome catalog add devrelaicom/midnight-expert-tome'},
  {kind: 'ok', text: '✓ cloned · {NUMBERS:plugins} plugins · {NUMBERS:entries} entries indexed'},
  {kind: 'cmd', text: 'tome query "audit a smart contract"'},
  {kind: 'ok', text: '→ {NUMBERS:top-hits-with-scores}'},
  {kind: 'out', text: 'loaded 1 skill · {NUMBERS:skill-tokens} tokens — the other {NUMBERS:entries-minus-one} stayed on the shelf'},
];
```

Below the terminal, the works-with strip: `works with Claude Code / Cursor / Codex / Gemini CLI / OpenCode`.

`Sprawl.tsx`: kicker `THE SPRAWL`; heading `Five agents. Five dialects. One you.`; body (italic serif, final): *“Your prompts, rules, and skills are pasted into `.claude/`, `.cursor/rules`, `AGENTS.md`, `GEMINI.md` — each copy drifting from the last. And the ones you do load sit in the context window all day, billing you for knowledge the agent isn’t using.”*; visual: a CSS-only column of five small file-tree cards each containing the same `review-checklist/SKILL.md` line with version annotations (`v1`, `v3`, `v2 (edited)`, `stale`, `?`) — no images.

`Method.tsx`: kicker `CHAPTER II — THE METHOD`; heading `Shelve it once. Read it everywhere.`; three plates: ① ACQUIRE / `tome catalog add <repo>`, ② BIND / `tome harness use cursor`, ③ CONSULT / `tome query "…"`, each a card with a one-line mono command; closing line: *“Inside a harness the same search runs over MCP — the agent pulls exactly the skill it needs, mid-task, by itself.”*

`Colophon.tsx`: kicker `COLOPHON — SPECIFICATIONS OF THIS VOLUME`; four plates (final copy): **Local, entirely** / “index, embeddings & reranker on your disk — no telemetry, no cloud”; **One binary, in Rust** / “a single static binary; semantic search built in”; **Honest exits** / “every failure has its own exit code — scriptable, CI-friendly”; **Five harnesses** / “native config for each — rules, MCP, agents, hooks”.

`AuthoringOnramp.tsx`: kicker `CHAPTER III — WRITE YOUR OWN`; heading `Already have plugins? Convert them.`; left: *“Bring a Claude Code marketplace, a Cursor rules folder, or a loose pile of SKILL.md files — `tome convert` turns them into a catalog. `create` scaffolds new ones; `lint` keeps you publishable.”* + link → `/docs/authoring/convert`; right: a **static** (non-typing) terminal block with the real convert/lint capture excerpt.

`FinalCta.tsx`: dark ink panel; kicker `BEGIN`; heading `Add it to your bookshelf.`; mono install block `brew install aaronbassett/homebrew-tap/tome` with sub-line `or cargo install tome-mcp`; outline link `Quickstart → 4 commands` → `/docs/getting-started/quickstart`.

`index.tsx` (rewrite):

```tsx
import React from 'react';
import Layout from '@theme/Layout';
import Hero from '@site/src/components/landing/Hero';
import Sprawl from '@site/src/components/landing/Sprawl';
import Method from '@site/src/components/landing/Method';
import Colophon from '@site/src/components/landing/Colophon';
import AuthoringOnramp from '@site/src/components/landing/AuthoringOnramp';
import FinalCta from '@site/src/components/landing/FinalCta';

export default function Home(): React.JSX.Element {
  return (
    <Layout
      title="Tome — One bookshelf, every coding agent"
      description="One bookshelf for everything your coding agents know — read everywhere, loaded one skill at a time. Tome manages skills, commands, agents & hooks across Claude Code, Cursor, Codex, Gemini & OpenCode.">
      <main>
        <Hero />
        <Sprawl />
        <Method />
        <Colophon />
        <AuthoringOnramp />
        <FinalCta />
      </main>
    </Layout>
  );
}
```

- [ ] **Step 3: Rewrite `landing.module.css`** — grimoire styles for everything referenced above: `.section` (parchment, alternating `--tome-paper`/`--tome-paper-deep`), `.kicker` (mono, oxblood, letter-spaced), `.h1` (Newsreader, clamp(3.5rem, 9vw, 6.5rem)), `.stop` (oxblood), `.lede` (serif italic, --tome-ink-soft, max 46ch), `.btn`/`.btnSolid`, `.terminal` (ink bg `--tome-code-bg`, gold border, cream text, mono 14–15px, min-height reserving the full transcript so the loop doesn't reflow the page), `.termTitle` (mono, gold, letter-spaced — the "fig. 1 — …" caption), `.term_cmd`/`.term_out`/`.term_ok` (cream / muted gold / green `#7ec99a`), `.termPrompt`, `.termCursor` (block caret, `@keyframes` blink, disabled under `prefers-reduced-motion`), `.plates` (3- and 4-up responsive card grids), `.works` strip, scroll fade-in class `.fadeIn` driven by an IntersectionObserver added in each section (guarded by reduced-motion). All animation CSS sits inside `@media (prefers-reduced-motion: no-preference)`.

- [ ] **Step 4: Delete the retired components and image**

```bash
git rm src/components/landing/Highlights.tsx src/components/landing/HowItWorks.tsx \
  src/components/landing/SpecSheet.tsx src/components/landing/FeaturedCatalog.tsx \
  src/components/landing/GetStarted.tsx src/components/landing/AuthorOnramp.tsx \
  static/img/tome-exploded.png
```

- [ ] **Step 5: Build + eyeball**

```bash
pnpm typecheck && pnpm build && pnpm start
```

Expected: typecheck + build PASS. Eyeball: terminal types and loops; reduced-motion (toggle in OS or devtools emulation) renders the static transcript; both color modes; mobile width (responsive grids stack).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(site): grimoire landing — typed-terminal hero, sprawl, method, colophon, authoring, CTA"
```

---

### Task 14: Playwright e2e rewrite

**Files:**
- Modify: `e2e/smoke.spec.ts` (full rewrite)

- [ ] **Step 1: Rewrite the spec**

```ts
import {test, expect} from '@playwright/test';

test('landing renders the grimoire hero and all six sections', async ({page}) => {
  await page.goto('/');
  await expect(page.getByRole('heading', {level: 1, name: /Tome/})).toBeVisible();
  await expect(page.getByText('One bookshelf for everything your coding agents know')).toBeVisible();
  for (const t of [
    'Five agents. Five dialects. One you.',
    'Shelve it once. Read it everywhere.',
    'COLOPHON',
    'Already have plugins? Convert them.',
    'Add it to your bookshelf.',
  ]) {
    await expect(page.getByText(t, {exact: false}).first()).toBeVisible();
  }
});

test('terminal demo renders statically under reduced motion', async ({page}) => {
  await page.emulateMedia({reducedMotion: 'reduce'});
  await page.goto('/');
  await expect(page.getByText('tome catalog add devrelaicom/midnight-expert-tome').first()).toBeVisible();
  await expect(page.getByText('stayed on the shelf')).toBeVisible();
});

test('install command shown and nav reaches docs', async ({page}) => {
  await page.goto('/');
  await expect(page.getByText('cargo install tome-mcp')).toBeVisible();
  await page.getByRole('link', {name: 'Docs'}).first().click();
  await expect(page).toHaveURL(/\/docs\//);
});

test('docs sidebar shows the four chapters and navigates', async ({page}) => {
  await page.goto('/docs/getting-started/install');
  for (const ch of ['Ch. I — Getting started', 'Ch. II — Using Tome', 'Ch. III — Authoring', 'Ch. IV — Reference']) {
    await expect(page.getByText(ch).first()).toBeVisible();
  }
  await page.getByRole('link', {name: 'Converting'}).click();
  await expect(page).toHaveURL(/\/docs\/authoring\/convert/);
});

test('dark mode toggle switches to the midnight library', async ({page}) => {
  await page.goto('/');
  await page.getByRole('button', {name: /dark mode|color mode|switch between dark and light/i}).click();
  await expect(page.locator('html')).toHaveAttribute('data-theme', 'dark');
});
```

(If the reduced-motion test's exact strings drifted with the real captured numbers, match on the stable prefixes shown above — they are written to avoid the `{NUMBERS}` parts.)

- [ ] **Step 2: Run it**

```bash
pnpm test:e2e
```

Expected: 5 passed (builds the site first via the Playwright webServer; ~3 min).

- [ ] **Step 3: Commit**

```bash
git add e2e/smoke.spec.ts
git commit -m "test(site): e2e for grimoire landing, chapters nav, reduced motion, dark mode"
```

---

### Task 15: CI workflow

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: Write the workflow** (mirrors the pnpm setup in `supply-chain-check.yml` — same action versions, Node 22):

```yaml
name: CI
on: [push, pull_request]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Doc drift check
        run: pnpm drift:check
      - run: pnpm typecheck
      - run: pnpm build
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: E2E
        run: pnpm test:e2e
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci(site): build + drift + typecheck + e2e workflow"
```

---

### Task 16: Final verification sweep + PR

**Files:** none new

- [ ] **Step 1: Full local gate**

```bash
pnpm drift:check && pnpm typecheck && pnpm build && pnpm test:e2e
```

Expected: all PASS. Paste outputs into the PR description later — no success claims without them.

- [ ] **Step 2: Spec checklist** — walk `specs/2026-06-10-site-overhaul-design.md` section by section and confirm: §3 every audit item fixed (grep the built site for `plugin.json` — remaining mentions must only be legacy/convert context; grep for `TODO(launch)` → zero; grep for `lend`/`lent`/`loan` → zero); §4 all 19 pages exist and are in the sidebar; §6 all six landing sections; §7 only Newsreader downloads (check `pnpm why @fontsource/space-grotesk` fails); §8 no `/blog` route in `build/`; §9 drift scripts run in CI; §10 contrast values recorded. Fix anything that fails before opening the PR.

- [ ] **Step 3: Push and open the PR**

```bash
git push -u origin site-overhaul
gh pr create --title "feat(site): docs accuracy + grimoire redesign" --body "$(cat <<'EOF'
One-branch overhaul per specs/2026-06-10-site-overhaul-design.md:

- Docs: Phase 8/9 surface documented (authoring create/convert/lint, tome skill, tome meta, MCP meta tool), exit codes through 89, tome-plugin.toml cutover corrected, four-chapter IA, every page rewritten with real captured CLI output.
- Landing: grimoire rebuild — typed-terminal demo, sprawl/method/colophon/authoring/CTA sections.
- Theme: parchment + midnight-library palettes, Newsreader display, reading-room docs.
- Blog removed (no redirects, per spec). Footer/navbar refreshed.
- Drift prevention: specs/reference/cli-surface.json + check-doc-drift.mjs in CI.

Verification: drift ✓ / typecheck ✓ / build (onBrokenLinks:throw) ✓ / e2e ✓ (outputs below).

Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR opens; Netlify preview + CI run. Review the Netlify preview visually (both color modes, mobile) before merging.

---

## Self-review notes (kept honest)

- The build is intentionally RED between Task 2 Step 5 (file move breaks `sidebars.ts`) and Task 11 Step 5 (IA flip). Tasks 4–10 verify with `pnpm drift:check` only. If you need a green build mid-stream, do not — finish Task 11.
- Docs-page tasks specify required facts + structure + voice, not full prose; the prose is written at execution time following spec §5 and the §Conventions voice block. All numbers/output come from `specs/reference/captures/` — if a capture is missing, capture it (Task 3 pattern), never invent.
- `prismThemes.oneDark` + `--prism-background-color` override: if the override selector doesn't take at runtime, set the background via the prism theme object spread in `docusaurus.config.ts` instead — verify in `pnpm start`, don't ship blind.
